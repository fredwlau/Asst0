First Assignment for Systems Programming CS214

Command line program to parse and sort input into tokens.  Uses nonalphabetic characters as delimiters.  Compile with:

gcc -g -Wall -stringsorter.c -o stringsorter
